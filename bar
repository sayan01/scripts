#!/bin/bash
if [ -e ~/.config/bar ]
then
	commands=$(cat ~/.config/bar)
else
	commands="date"
fi
baritems(){
	sep="|"
	for com in $commands
	do
		echo "`$com` $sep"
	done
}
baritems | tr '\n' ' '
echo
