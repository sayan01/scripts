#!/bin/bash

cd ~/scripts/ || exit 1
file="$(find -- * -executable -type f | rofi -theme ~/.local/share/rofi/themes/slate.rasi -dmenu -i -scroll-method 1 -p scripts)"

if [ "$file" ]
then
	ixon="stty -ixon &&"
	if [ -f "$HOME/scripts/$file" ]
	then
		terminator -e "$ixon vim $HOME/scripts/$file"
	else
		terminator -e "$ixon $HOME/scripts/initscript $file"
	fi
fi
