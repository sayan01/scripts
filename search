#!/bin/bash

input=$(echo | rofi -theme ~/.local/share/rofi/themes/flat-orange.rasi -dmenu -i -scroll-method 1 -p ' search') 

if [[ -n "$input" ]]
then
	chromium "google.com/search?q=$input" &
fi
